<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Document
    style.
      .flex {
        display: flex;
      }
      .block {
        width: 300px;
        height: 100px;
        background: gray;
        margin: 12px;
      }
      iframe {
        width: 100%;
        height: 100%;
      }
  body
    .flex
      .block
        iframe(src = '/status1')
      .block
        iframe(src = '/status2')
      .block
        iframe(src = '/status3')
      .block
        iframe(src = '/status4')
      .block
        iframe(src = '/status5')
      .block
        iframe(src = '/status6')
      .block
        iframe(src = '/status7')

    .flex 提示: 打开开发者面板 Network 标签, 查看页面状态码
  script(src="/axios")

  script.
